import { AssessmentResult } from './assessment-result.model';

describe('AssessmentResult', () => {
  it('should create an instance', () => {
    expect(new AssessmentResult()).toBeTruthy();
  });
});
