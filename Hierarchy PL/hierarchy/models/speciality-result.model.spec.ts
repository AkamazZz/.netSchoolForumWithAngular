import { SpecialityResult } from './speciality-result.model';

describe('SpecialityResult', () => {
  it('should create an instance', () => {
    expect(new SpecialityResult()).toBeTruthy();
  });
});
