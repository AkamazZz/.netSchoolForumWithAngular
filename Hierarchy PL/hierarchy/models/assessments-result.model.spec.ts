import { AssessmentsResult } from './assessments-result.model';

describe('AssessmentsResult', () => {
  it('should create an instance', () => {
    expect(new AssessmentsResult()).toBeTruthy();
  });
});
