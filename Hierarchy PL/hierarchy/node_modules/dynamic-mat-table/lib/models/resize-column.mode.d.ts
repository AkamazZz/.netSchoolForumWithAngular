import { Subject } from 'rxjs';
export declare class ResizeColumn {
    startX: number;
    startWidth: number;
    isResizingRight: boolean;
    currentResizeIndex: number;
    resizeHandler?: 'left' | 'right';
    widthUpdate: Subject<{
        i: number;
        w: number;
    }>;
}
