import { EventEmitter } from '@angular/core';
import { ContextMenuItem } from '../../../models/context-menu.model';
import { TableSetting } from '../../../models/table-setting.model';
export declare class RowMenuComponent<T> {
    rowActionChange: EventEmitter<ContextMenuItem>;
    actionMenus: ContextMenuItem[];
    tableSetting: TableSetting;
    rowActionMenu?: {
        [key: string]: ContextMenuItem;
    };
    visibleActionMenus: ContextMenuItem[];
    constructor();
    menuOnClick(e: any): void;
    menuButton_OnClick(menu: ContextMenuItem): void;
}
